lib_LTLIBRARIES = libfractal.la

libfractal_la_SOURCES =
libfractal_la_LIBADD = core/libcore.la util/libutil.la probes/libprobes.la

includesubdir = $(includedir)/fractal

includesub_HEADERS = fractal.h

SUBDIRS = core util probes
